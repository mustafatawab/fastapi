[project]
name = "todo-with-db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]>=3.2.13",
    "sqlmodel>=0.0.27",
]
